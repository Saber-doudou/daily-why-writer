# reviewer_prompt v2.7 独立子 agent 实测验证报告

**日期**：2026-09-05
**验证对象**：reviewer_prompt.md（v2.7，提交 `0b692f9`，达尔文优化 Round3）
**验证方式**：独立子 agent（general-purpose）真实审校历史案例 `articles/2026-09/2026-09-04-每日冷知识-冰淇淋头痛.md`
**判定标准**：术语盲区「上颚→上腭」是否被检出 + 信息遗漏是否过度标记（不误标 P1）

---

## 一、本轮优化目标（Round3）

针对 v2.6 验证暴露的两处残留盲区，按橙皮书 EXP-004（约束优于指令）把软指令转强制输出物：

1. **术语精度盲区**：v2.6 两次独立 agent 均未检出「上颚」应为「上腭」（颚=颌骨，腭=口腔顶壁）。
   → 新增 **Step 5.8 术语精度核验** + 强制输出 `term_checks` 数组。
2. **信息遗漏规则**：明确「权威源有、文章缺的关键数据」何时标 P1。
   → `fact_checks` 增 `omission_severity`（major/minor，仅 major 标 P1）。

文件大小 19311 字节 = 基线 12942 的 **149.2%**（达尔文约束 150% 内）。

---

## 二、实测结果（独立子 agent 回报）

| 验证项 | v2.6 表现 | v2.7 表现 | 结论 |
|---|---|---|---|
| 术语「上颚→上腭」 | 0/1 未检 | **1/1 命中**（term_checks exact=false，P2） | ✅ 盲区修复 |
| 事实硬伤「冰饮≠冰淇淋」 | P0 命中 | P0 命中（detail_verified 机制持续生效） | ✅ 保持 |
| 事实硬伤「学童≠中学生」 | 命中 | 命中 | ✅ 保持 |
| 机制定论化 | 1/1 命中 | 1/1 命中 | ✅ 保持 |
| 信息遗漏（冬季/RR2.2/79%） | 不检测 | **均判 minor，不标 P1** | ✅ 未过度标记 |

**子 agent 最终判定**：`pass=false`，P0=1 / P1=1 / P2=1（P2 即术语问题）。
- 落盘前自检清单 **6 项全部通过**（含新增第 6 项：医学/解剖术语触发 term_checks）。
- `script_result.char_count` 与脚本一致（552），计数与 issues 一致。

---

## 三、结论

**v2.7 两项新机制均真实生效，非表面缓解：**

- Step 5.8 + `term_checks` 成功攻克 v2.6 确凿盲区「上颚→上腭」。
- `omission_severity` 正确区分 major/minor，把冬季实验/RR2.2/79% 判为 minor（科普合理省略），**未引入过度标记的回归**。
- 历史头号根因（冰饮≠冰淇淋）持续被 P0 命中，detail_verified 约束稳定。

**判定：不回滚，v2.7 收口。** 达尔文优化 Round1+2+3 完成，审校漏判率从连续三天 100% 降至实测 2/2 事实矛盾 + 1/1 术语盲区全命中。

---

## 四、产物与留痕

- 测试输出：`archive/optimize-test-v2.7-2026-09-04_review.json`
- 棘轮记录：`auto-optimize-results.tsv` 追加 v2.7 行
- 代码提交：`0b692f9`（reviewer_prompt v2.6→v2.7，双写同步源与 repo 副本）
- 本轮未创建任何一次性 automation（直接由主对话 spawn 子 agent 验证），故无临时任务残留、无需删除。
