---
title: "daily-why-writer"
summary: "每日冷知识文章写作规范 — 结构、排版、风格"
agent_created: true
---

# daily-why-writer

每日冷知识（"每日一个为什么"）文章的写作 skill。包含结构、排版、风格规范。

**规则来源**：`F:\WorkBuddy\daily-why\writing_rules.json`（唯一来源，所有数值规则以此为准）

## 元规则（Meta-Rules）

1. **教训没有不可泛化的** — 如果觉得不可泛化，是提取粒度不够
   - 每次出错都要分析根因，提炼为可复用的规则
   - 记录到 Feedback Log，转化为规则

2. **文件架构三层分离** — 法典/判例/索引
   - `SKILL.md` = 法典（核心写作规范，唯一真相源）
   - `references/CHECKLIST.md` = 自检清单（写后检查，按需加载）
   - `references/FORBIDDEN.md` = 黑名单 FP-01~12（禁止模式，按需加载）
   - `references/FEEDBACK_LOG.md` = 教训记录（按需检索）
   - `review/CASE_STUDIES.md` = 判例库（详细案例，审校时 Grep）
   - `topics_context.json` = 索引（选题去重）

3. **判例按需检索** — 不预加载，审校发现 P0/P1 时 Grep
   - 自动化 prompt 不预加载判例库（节省 token）
   - 审校发现问题时，搜索相关判例参考修正方案

## 内容结构（A+C+F）

文章必须按以下三段式结构组织：

### A = 故事化开头（Anecdote）
用一个场景或生活化小故事切入，引出话题。

**✅ 好的例子：**
> 清晨的阳台上，一只橘猫正懒洋洋地打盹。突然一个翻身——"喵呜！"——它从窗台滑了下去。就在你以为要出事的时候，它优雅地在空中扭了个身，稳稳落地。

**❌ 反面例子：**
- "今天我们来聊聊猫为什么能四脚着地"
- "你知道吗，猫有一种神奇的反射"
- "随着科技的发展，人们发现..."

### C = 疑问驱动解释（Question-driven）
围绕核心问题，用 **3 个递进式问题** 引导读者思考并给出答案。

**Q 标记格式（统一用加粗，不用 h3）：**
```
**Q1：第一个问题是什么？**

答案内容...

**Q2：接下来的第二个问题？**

答案内容...

**Q3：更深一层的问题？**

答案内容...
```

**要点：**
- 统一用 `**Q1：xxx？**` 加粗格式，**禁止**用 `### 🤔 Q1` 的 h3 格式
- 每个问题后紧跟答案和解释
- 问题之间递进深入（从基础 → 进阶 → 延伸/辟谣）
- 每个段落解释一个问题，不要把多个问题混在一起
- Q 段之间用**空行**分隔，**不要加 `---` 分隔线**
- 用加粗 **突出** 关键概念
- **⚠️ Q段之间的逻辑必须自洽**（2026-06-02 教训）：如果 Q1 说"A功能不存在"，Q2 又说"A功能很重要"，读者会困惑。必须在 Q1 或 Q2 中搭桥解释（如"在干燥条件下不防滑 ≠ 在所有条件下不防滑"），消除表面矛盾
- **⚠️ F段不能与Q3内容重复**（2026-06-02 教训）：冷知识反转应提供**新角度**，不能只是换一种说法重复Q3已经讲过的内容。如果Q3已经讲了"基因+环境"，F段应从更宏观的角度切入（如"这个机制遵循了某个著名数学模型"）
- **⚠️ F段长度需与Q段均衡**（2026-06-05 教训）：F段不能只有一两行，应与Q段长度大致相当。如果F段明显偏短，考虑补充相关延伸知识
- **⚠️ C段开头避免学术化句式**（2026-06-05 教训）："这个说法流传了几十年，但科学家早就把它推翻了"太学术化。改为更口语化的切入（如"听起来挺合理，但早就被实验证伪了"）

### F = 趣味反转结尾（Fun twist）
在结尾揭示一个令人意外的事实，必须用**引用块**包裹。

**统一格式：**
```
> emoji **冷知识反转**：反转内容...
```

**✅ 好的例子：**
> 🐾 **冷知识反转**：即使在完全漆黑的环境里，猫依然能精准四脚着地——靠的是内耳里的"**天然陀螺仪**"，盲猫也能做到。

**💡 收尾技巧**：反转之后可加一句闭环收尾，增强记忆点。如"脚麻，是沉默的神经系统难得的'广播故障'——却恰好让我们听见了它的存在。"

**❌ 反面例子：**
- "总之，这是一个有趣的现象。"
- "希望这篇文章对你有所帮助。"
- 用"冷知识彩蛋"替代"冷知识反转"（必须统一用"冷知识反转"）
- 反转内容没有用 `>` 引用块包裹
- 把彩蛋藏在风格表格里而不是独立成段
- F 段用词拗口、生造搭配（如"留下一脚不太优雅的绊"），应优先用自然口语词（如"打了个不太优雅的趔趄"）

## 排版格式（唯一权威，所有排版规则以此为准）

| 项 | 要求 | 示例 |
|---|---|---|
| **标题** | emoji + 疑问句 | `# 🐱 为什么猫会四脚着地？` |
| **Q 格式** | 统一 `**Q1：xxx？**` 加粗，**禁止** h3 | `**Q1：为什么xxx？**` |
| **分隔线** | 恰好 2 处，A段后 + F段前，C段内部 Q 之间**不加** | `---` |
| **引用块** | A 段和 F 段必须用 `>` 包裹，C 段不用 | `> 想象一下...` |
| **F 段标签** | 统一 `> emoji **冷知识反转**：内容`，禁止"冷知识彩蛋" | `> 🐾 **冷知识反转**：...` |
| **加粗** | `**` 突出 2-3 个关键概念 | `**翻正反射**` |
| **风格表格** | 结尾附统一 4 行模板（话题/分类/核心机制/冷知识反转），**分类行必须从 6 个标准分类中选一个** | 见结构模板 |

### 文章结构模板（必须严格遵守）

```markdown
# emoji 为什么xxx？

> A段：场景描写，用引用块包裹...

---

**Q1：第一个问题？**

答案段落...

**Q2：第二个问题？**

答案段落...

**Q3：第三个问题？**

答案段落...

---

> emoji **冷知识反转**：反转内容，用引用块包裹...

| 话题 | 为什么xxx？ |
|------|------------|
| 分类 | 人体奥秘/自然科学/生活常识/宇宙探索/动物世界/物理化学 |
| 核心机制 | 一句话概括原理 |
| 冷知识反转 | 一句话概括反转 |
```

> ⚠️ 以上模板中的分隔线、引用块、Q 格式、风格表格行均需严格遵守排版格式表中的要求。
> ⚠️ **分类行必须填写**，从以下 6 个标准分类中选一个：人体奥秘 / 自然科学 / 生活常识 / 宇宙探索 / 动物世界 / 物理化学。禁止留空或使用非标准分类。

## 语言风格

- **语气**：轻松愉快、生动有趣、互动性强、有记忆点
- **字数**：**300-600 字**（中文字符，不超 600；精确阈值见 `writing_rules.json`）
- **话题来源**：自然科学 / 生活常识 / 人体奥秘 / 宇宙探索
- **话题去重**：已用话题见 `topics_context.json` 的 `topics[].topic`，从其中选一个**未曾使用**的
- **读者**：普通大众，不是学术论文读者
- **原则**：避免过于学术化表达，增强场景感和代入感
- **记忆点**：每篇文章要有一个能让读者记住的"啊哈时刻"

## 参考流程

标准生产流程：
```
选题 → WebSearch 查证（1-2 个关键词）→ 写作 → 自检清单 → 保存 → validate_article.py 审核 → update_history.py
```

### 规则同步（修改规则后必做）
当修改 `writing_rules.json` 或本 SKILL.md 中的规则后，运行以下命令同步 automation prompt：
```bash
C:/Users/admin/.workbuddy/binaries/python/versions/3.13.12/python.exe F:/WorkBuddy/daily-why/generate_prompt.py --backup
```
这会从 writing_rules.json + SKILL.md 自动生成最新 prompt，消除手动同步的不一致问题。

### 写前查证（WebSearch）
选题确定后，用 WebSearch 搜 1-2 个该话题的关键词，目的是：
- 查证文章中的关键数字是否准确（距离、倍数、温度等），有争议的表述要修正
- **核实引用的机构名和研究者姓名是否正确**（如"英格兰大学"不存在，应为"曼彻斯特大学"）
- **搜索该话题的最新科学机制**，避免引用已被更新的旧解释（如搜"XX 机制 最新研究"）
- **⚠️ 研究者引用必须完整**（2026-06-05 教训）：首次提及研究者时，需标注"机构+年份+发表期刊"（如"杜伦大学的纳贾·赖斯兰团队（2012，PLOS ONE）"）。后续提及可省略出处
- 看看同类科普是否写了辟谣/误区内容，如有则参考融入
- 确认反转是否够独特（别人没写才保留）
- **注意**：搜 1-2 个结果即可，不用深入阅读超过 2 篇。纯常识类话题（如海水咸、天蓝）可跳过

### 写后自检
文章写完后，按 `references/CHECKLIST.md` 逐项检查，重点关注**机制描述准确性**（高危区）

审核通过条件：**P0=0 且 P1≤2**
- P0 致命：事实错误、逻辑矛盾、结构缺失、**Q用h3格式、分隔线>3处** → 必须清零
- P1 重要：AI 味、格式缺陷（分隔线恰好3处等） → ≤ 2 个可放行
- P2 一般：措辞微调、风格表格行标题不完整 → 不设限

## 风格样本

一篇合格文章应该：
1. 读者看标题就想点（emoji + 疑问）
2. 读完第一段就被带入场景
3. 被 3-4 个问题牵着走
4. 结尾被反转"打脸"
5. 最后笑一下，记住一个冷知识
